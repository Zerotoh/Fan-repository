# 个人跑步看板 · 代码与设计评审

评审时间：2026-09-22
评审范围：`index.html`、`scripts/analyze.py`、`scripts/sync.py`、`refresh.bat`、`data/plan_config.json`、`data/raw/*`、`.workbuddy/memory/*`、两个自动化任务

---

## 总体结论

架构选型是对的：零依赖、纯前端、`file://` 双击即用、数据用 `window.DASHBOARD_DATA` 剖开 fetch 限制——这些都是刻意的取舍，不是凑合。训练计划的周期化 + 上二休二 + 长距离疲劳规则，也已经超出"玩具看板"的水平。

问题集中在三块：**数据可靠性（会真的丢数据）**、**指标口径（会让你误判自己的状态）**、**看板可用性**。按优先级列在下面。

---

## P0 · 数据可靠性

### 1. sync.py 会把"失败"当成"成功"写盘 ⚠️ 已实际造成数据损坏

- 证据：`data/raw/_corrupted_backup_20260922071054/` 里 4 个文件都是 97 字节
  `{"status": "error", "message": "garminconnect 未安装。请确保通过 uv 运行此脚本。"}`
- 根因：`sync.py:42-58` 的 `run()` 只校验输出**是不是合法 JSON**，不校验**内容是不是成功**。
  CLI 失败时输出的错误桩恰好是合法 JSON → 通过校验 → 覆盖掉本地真实数据。
- 修复方向：
  1. 落盘前校验 `status == "success"` 且 `data` 非空，否则不写、保留旧文件；
  2. 写盘改成临时文件 + `os.replace()` 原子替换（现在 `open(w)` 中途崩就是半个文件）；
  3. 校验数据合理性（如活动条数比上次少 50% 就中止）；
  4. 失败时 `sys.exit(1)`，让自动化能感知"这次没成"。

### 2. 两个自动化重复且同一时刻触发

- `355d240a`（每日刷新跑步看板）和 `5e3472f8`（佳明数据每日同步）都是每天 07:00。
- 9/22 早上的结果：一个跑成功了，另一个用错解释器报"garminconnect 未安装"并把 raw 写坏，两者互相踩。
- 建议：合并成一个自动化，串联 `sync → analyze`；确实要保留两个就错开时间（例如 06:50 / 07:05）。自动化里必须显式用
  `C:\Users\Fan\.workbuddy\binaries\python\envs\garmin-cn\Scripts\python.exe`（已验证该环境 `import garminconnect` 正常），不要依赖默认 python。

### 3. 当前 raw 里的健康/睡眠数据是"重建"出来的，不是真同步

- `.workbuddy/memory/automations/355d240a.../memory.md` 记录：9/22 07:10 用 9/21 的 dist 快照反向重建了 `health_90d.json` / `sleep_90d.json`。
- 影响：`summary_today.json` 是真值，但健康/睡眠的近 7 天细节可能是派生值 → 就绪度评分、状态信号里混有合成成分。
- 建议：修好第 1 条后立刻真跑一次 `sync.py`，把 raw 换成真数据，再比对一次就绪度有没有变化。

### 4. 每次全量重拉，其实是增量就够

`sync.py:77` 用 `--start 2010-01-01` 每天拉 207 条历史活动。改成从 `activities_running.json` 里最后一次日期往前推 7 天开始拉、和旧数据合并去重，能省掉大部分请求，也降低被限流的概率。

### 5. 缺依赖锁定（这次事故的根源）

`garminconnect` 没有版本约束，也没有 `requirements.txt`。环境一变就炸。建议加：

```
# requirements.txt
garminconnect==0.3.16
```

外加一份三行 README：数据从哪来、怎么刷、刷失败了怎么办。

---

## P1 · 指标口径（这部分直接影响你对自己状态和能力的判断）

### 6. ACWR 的窗口是 4 周 / 12 周，而且互相嵌套

`analyze.py:199-207`：急性 = 近 4 周里程 ÷ 4，慢性 = 近 12 周里程 ÷ 12。标准 ACWR 是 **7 天 / 28 天**。现在这个口径天然趋近 1，敏感度被稀释，而且和 Garmin 自己给的 `ratio`（0.9）口径不同，两个数并排显示会让人不知道该信哪个。

还有一个隐患：窗口结束点是 **最后一次跑步的日期**，不是今天。你 9/21 之后一直不跑，急性负荷也不会衰减——"当前负荷"其实反映不了当前。

建议：改成 7d/28d，窗口右端锚定"今天"，并在前端标注"自算 ACWR"和"Garmin ACWR"的区别。

### 7. "周里程波动 CV" 实际算的是月里程

`analyze.py:212-217` 里 `wk = a["date"][:7]` 取的是**月份**，变量名 `wk_dist`、字段名 `weekly_dist_cv`、UI 文案"周里程波动 CV"全都写着"周"。要么改成真正按 ISO 周分桶，要么把名字和文案改成"月"。

### 8. 恢复维度：量纲混算 + 描述与分数不一致

- `analyze.py:357-364` 先用静息心率算 `s_recovery`；
- `analyze.py:451-458` 又把它整个覆盖成 `mean(HRV 近7天均值, 睡眠评分)`。

HRV 的原始值（毫秒，你这边 109~114）和 0–100 分的睡眠评分直接取平均，量纲不同；而且维度描述里还写着"静息心率 39 bpm"，跟实际分数来源已经无关了。

建议：三个来源分别归一化再加权，例如 `RHR 0.4 + (HRV/个人基线) 0.3 + 睡眠评分 0.3`，描述里如实写出来。HRV 相对基线的偏离（Garmin 已给 `baseline_low/upper`）比绝对值有意义。

### 9. 比赛估算偏乐观，且混入了跑步机数据

- `analyze.py:258`：同一距离区间里取所有候选的 **min**，等于把"某次条件理想的表现"当成能力基线，长期偏乐观（你现在 1h55m 的预计就是这么来的）。改成按最近 12 周、按距离加权或取分位数更稳。
- `analyze.py:185`：`valid` 包含 `treadmill_running`（21 次）。跑步机配速和心率不可比，会污染 `best_pace`、`overall_pace`，进而污染阈值配速和五档配速区间。
  建议配速类指标**只用户外跑**（或至少分开统计）。

### 10. 目标进度缺"时间维度"——这是最该补的一块

现在只显示"预计 1h55m，还差 5 分"这一个快照。你真正关心的是：**在往 1:50 收敛吗？**

建议：每天把 `{date, 半马预计sec, 阈值配速, 周里程}` 追加进 `data/processed/goal_history.jsonl`，看板上画一条向 1:50 逼近的曲线（附一条目标线）。有了这条曲线，"这周该加量还是该收"才有依据。

---

## P2 · 看板可用性

11. **分段配速图在长距离时标签打架**：`index.html:420-441` 的 `buildLine` 固定 `W=340`，18km 就是 18 个 x 标签。建议抽稀（每隔 2–3 个点显示一个）或换成带 tooltip 的柱状图。
12. **备班日被显示成"休息日"**：`index.html:606-608`。你的作息里备班日是**可以训练**的，空着的时候提示"可自行加 30–40 分钟轻松跑"比"休息日"有用得多。
13. **90 天的睡眠 / HRV / 身体电量 / 负荷只有数字没有图**：raw 里 90 天数据都在，加一张多指标趋势图性价比极高，也能看出"睡眠是不是在变差"。
14. **排课缺恢复间隔约束**：`analyze.py:710-722` 先等间隔挑日子，再按 priority 顺序填模板，长距离可能落在周中、强度课也可能挨着。建议加硬约束：两次质量课间隔 ≥48h、长距离次日不排质量课。
15. **`runs_per_week=1` 会除零**：`analyze.py:711` 的 `/(n-1)`。当前配置是 4 所以没暴露，但改配置就会崩，属定时炸弹。
16. **在线链接落后于本地**：`dist/` 是 9/21 13:03 的快照，本地数据是 9/22 07:14。手机上看的是旧数据，容易误判。要么把"刷新数据 + 重新发布"做成一个有授权的流程，要么在页面顶部显示"快照时间"，并和数据时间对比出提示。
17. **页面没有"数据是否新鲜"的强提示**：现在只有一行小字 `数据更新于 …`。建议超过 48h 未更新时变红/加警示条——毕竟是驱动训练决策的东西。

---

## 建议动手顺序

| 顺序 | 内容 | 理由 |
|---|---|---|
| 1 | 修 sync.py 校验 + 原子写（#1），合并两个自动化（#2） | 先止血，已经丢过一次数据 |
| 2 | 真跑一次 sync，换掉重建的 raw（#3） | 让当前结论可信 |
| 3 | 修 #7 #8 两个口径 bug | 改动小、影响直接 |
| 4 | 上 #10 目标进度曲线 | 对"跑进 1:50"这个目标收益最大 |
| 5 | #11 #12 #13 体验优化 + #14 #15 排课/健壮性 | 日常用起来更顺 |

---

## 2026-09-22 落地情况（当天已修）

**P0（数据可靠性）**

- ✅ #1 `sync.py` 已重写：校验 `status`/`data` 内容（错误桩直接丢弃、不落盘）、写盘改为临时文件 + `os.replace()` 原子替换、覆写前备份一代到 `data/raw/_backup_prev/`、单次重试、失败退出码 1。
- ✅ #2 两个 07:00 的自动化已合并为一个（`5e3472f8`，名字改为"每日刷新跑步看板（Garmin 同步 + 分析）"），显式指定 `garmin-cn` venv 的解释器；原来用错 python 的那个（`355d240a`）已删除。
- ✅ #3 已用真实同步覆盖重建数据：`sync.py` 跑通 4/4（+ 最新跑步明细），`data/raw` 现为真值。
- ✅ #4 活动改为增量拉取（从最后一次跑步前 7 天开始 + 按 `activity_id` 合并去重），不再每次拉 2010 年至今的全量。
- ✅ #5 新增 `requirements.txt`（`garminconnect==0.3.16`）与 `README.md`（数据链 / 怎么刷 / 刷失败怎么办 / 配置说明）。

**P1（指标口径）**

- ✅ #6 ACWR 改为标准 **7 天 / 28 天**，窗口右端锚定"今天"（不再锚定"最后一次跑步"，不跑的日子负荷会正常衰减）。前端标注为"自算"以区别于 Garmin 的 `ratio`。
- ✅ #7 "周里程波动 CV" 改为真实 ISO 周口径（近 12 个完整周，含 0 里程周，中断会体现在 CV 上）。
- ✅ #8 恢复维度改为 **RHR 0.4 + HRV(相对个人基线) 0.3 + 睡眠 0.3**，三者分别归一化后加权；维度描述同步为"静息心率 39 bpm · HRV 112（基线 107） · 睡眠 64"，不再出现"分数与描述来源不一致"。
- ✅ #9 配速类指标只算户外跑（跑步机排除）；比赛估算改为"各区间最有力的一次实测努力 + 时间贬值（约 0.5%/月，上限 8%）"，并用短距离证据按 Riegel 外推交叉验证取更有力一侧。半马预计由 2h19m（误把一次轻松 18km 当半马能力）修正为 **1h59m**，并在前端标出依据（"由 10K 外推"或"依据 X 月 X 日实测"）。
- ✅ #10 新增目标进度时间序列 `data/processed/goal_history.jsonl`（每日一行，同日覆盖），看板"每周课表"页新增「目标进度」曲线 + 1:50 目标线 + 与首次记录相比的快/慢。

**P2**

- ✅ #11 分段配速图加宽并做 x 轴标签抽稀（18km 不再挤成一团）。
- ✅ #12 备班日未排课时显示"备班日 · 未排课 / 可自行加 30–40 分钟轻松跑"，不再和休息日混为一谈。
- ✅ #13 新增「近 60 天趋势」：睡眠评分 / 睡眠时长 / 静息心率 / HRV 四张小折线 + 区间最低最高。
- ✅ #14 排课改为穷举打分：先最大化**质量课之间**的最小间隔（≥48h），轻松跑主动填空位。本周示例由「一 T / 三 L / 四 FL」改为「一 T / 三 E / 四 L / 日 FL」。
- ✅ #15 `runs_per_week=1` 的除零已随 #14 重写一并消除。
- ✅ #17 数据超过 36 小时未更新时，页面顶部出现红色警示条。
- ⏳ #16 在线链接仍是 9/21 快照（`dist/`）。重新发布需要你授权，没有静默部署。

**验证**：`analyze.py` 跑通；Node + DOM mock 冒烟测试通过（14 项渲染检查全绿）。

---

---

# 第二轮评审（2026-09-25）

评审范围：`index.html`、`scripts/analyze.py`、`scripts/sync.py`、`.github/workflows/daily.yml`、`.gitignore`、生成产物 `dashboard_data.js`、自动化状态、`git ls-files`。

结论：**看板的算法部分已经相当成熟，这一轮的问题几乎全在"数据链路"和"时间锚点"上——也就是"你看到的东西是不是今天的、是不是对的"。** 其中 3 条是正在发生的故障，不是隐患。

---

## P0 · 正在坏的三件事

### A. 三条链路并行写同一份产物，CI 那份是残缺的

当前同时存在三个写手：

| 写手 | 频率 | 状态 |
|---|---|---|
| 本地自动化 `5e3472f8` | 每日 07:00 | **已 PAUSED** |
| GitHub Actions `daily.yml` | 北京 07:13 **+** 07:43（一天两次） | 活跃，且会 `git push` 回仓库 |
| 手动 `scripts/refresh.bat` | 不定期 | — |

**已实际造成降级**：仓库里 9/23 那版 `dashboard_data.js`（`generated_at: 2026-09-23 14:20`）中的 `plan_config` 是：

```json
{"goal":"half_marathon_pb","goal_label":"半程马拉松 PB","race_distance_km":21.0975,
 "race_date":null,"runs_per_week":4,"work_pattern":"2on2off","work_anchor":"2026-09-22"}
```

对照本地 `data/plan_config.json`（有 `goal_target_time: "1:50:00"`、`goal_target_minutes: 110`、`goal_label: "半马跑进1:50"`、`race_date: "2027-01-01"`）——**四个目标字段全丢了**。

根因：`data/plan_config.json` **从未入库**（`git ls-files` 里没有它，git status 显示 `??`）。CI 在 runner 上 checkout 后读不到这个文件，`load_plan_config()` 静默回落到 `defaults`（`except Exception: pass` 把错误吞了）。

连带后果（前端全链路失效）：
- `plan.goal_target_time` / `goal_target_minutes` / `goal_gap_min` / `weeks_left` 全为 `null`
- 「目标与周期」栏不显示"目标完赛 1:50:00"，不显示"还差 9 分"
- **目标进度图的 1:50 绿色目标线画不出来**（`renderGoal` 里 `goalSec` 为 null）
- 周期阶段显示成"永续强化期"而不是"基础期(打底盘)"，距比赛周数不显示

修法（建议三条全做，成本都很低）：
1. `git add data/plan_config.json` —— 里面没有敏感信息，本就该入库；
2. `analyze.py` 的 `load_plan_config()` defaults 补上 `goal_target_time` / `goal_target_minutes`，并把 `except Exception: pass` 改成打印告警（现在丢配置是静默的）；
3. `daily.yml` 里两条 cron 删掉一条（07:43 那条纯属重复，还多一次 Garmin 登录、多一次 push 冲突风险）。

> 顺带：如果主人其实只看本地看板，最干净的做法是**关掉 CI 的 schedule**（保留 `workflow_dispatch` 手动触发），让 GitHub Pages 只作为"我手动推一版上去"的发布通道，避免 CI 用残缺配置把本地数据覆盖回去。

### B. 本地自动化处于 PAUSED，数据已断更 2 天

`5e3472f8`（每日 07:00 同步+分析）当前状态 `PAUSED`。`goal_history.jsonl` 最后一行是 2026-09-23，今天是 09-25。看板顶部因此挂着"数据已 XX 小时未更新"的红条。

需要决定：恢复本地自动化，还是干脆交给 CI。

### C. analyze.py 在 Windows GBK 控制台会崩

`data/processed/_sync_last.log` 末行：

```
[sync] 分析步骤失败: 'gbk' codec can't encode character '\u26a0' in position 2: illegal multibyte sequence
```

`analyze.py:1505` / `1510` 两处 `print` 含 `⚠`（长距离疲劳规则触发时、`实际超量⚠`）。子进程 stdout 走 GBK → `UnicodeEncodeError` → analyze 非零退出 → sync 判定整体失败。

**这意味着：任何一天触发了长距离疲劳规则，当天同步就会失败。** 属于定时炸弹，不是偶发。

修法（两条都做）：
- `analyze.py` 顶部加 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`；
- `sync.py` 给子进程 env 加 `PYTHONIOENCODING=utf-8`。

### D. 「今天」跟着数据走，不跟着日历走 ⚠ 影响最大

`analyze.py:1382`：

```python
today = summary.get("date") or datetime.now().strftime("%Y-%m-%d")
```

`summary_today.json` 里的 `date` 是 **2026-09-22**（最后一次成功同步那天的快照）。今天 09-25，于是：

- `plan_week` 里 `is_today` 打在 **09-22** 上（主班日）；
- 「今日训练建议」显示的是三天前的课表，日期写 2026-09-22；
- 「本周课表」高亮错格；
- `goal_history` 因为没有"今天"这个日期，也不会追加新点。

**同步一断，看板就静默停在过去，还假装是今天。** 这比"数据旧"更危险——你会照着三天的课表执行。

修法：`today` 用 `datetime.now()`（真实日历），summary 的日期单独存为 `state_date`。当 `state_date < today` 时：
- 页头明确写"状态/睡眠/电量数据来自 09-22 快照，今日课表按最新可得数据推算"；
- 就绪度打分里标注哪些因子是旧数据。

---

## P1 · 指标可信度

### E. 综合评级被昨夜睡眠左右（一天掉一级）

`goal_history`：`09-22 → composite 67 (B)`，`09-23 → 62 (C)`。一天掉 5 分、掉一个等级。掉的这两项都不是能力变化：

- recovery 维度（权重 0.25）= RHR + **近 7 天 HRV + 近 7 天睡眠评分** → 睡眠 59、电量 12 拉低；
- aerobic 维度（权重 0.30）里含 **当前 ACWR**（0.7 → 只给 +5 分）。

结果是"没睡好 → 评级从 B 掉到 C"，会让人误以为自己退步了。

修法：拆成两个数并列显示——
- **能力分**：只由比赛成绩 / VO2 Max / 慢性周里程 / 规律性构成，用 4–8 周窗口，日间几乎不动；
- **状态分**：现在的 composite，日更，标注"今日状态"。

或对维度分做 EMA 平滑 + 单日变化限幅（±2 分）。

### F. 半马估算的证据已经 52 天了

`race_estimates.半马`：`basis=derived, derived_from=10K`，而 10K 的 `evidence_date` 是 **2026-08-04**。前端只写"由 10K 外推"，不说证据多旧。

修法：加 `evidence_age_days`；>45 天时前端显示黄色提示"依据偏旧（X 月 X 日的 10K），建议近期跑一次 10K 或半马实测校准"，并给出置信度（high/mid/low）。

### G. 缺"我在变强吗"的最灵敏指标：跑步效率 EF

raw 里每次活动都有 `avg_hr` 和配速，可以算 **效率指数 EF = 速度(km/h) ÷ 平均心率(bpm)**（或 pace/HR），取 4 周移动均值。

同配速下心率下降 = 有氧在进步，比 VO2 Max 灵敏得多，也是周度最值得看的一条曲线。建议在"历史分析"加一张 EF 趋势图，跑后分析里也显示本次 EF 与近 8 次均值对比。

### H. ACWR 之外缺一个"今天能不能硬"的合成结论

Garmin 已给 `acute_load 289 / chronic_load 362`，可直接算 **TSB/Form = 体能 − 疲劳**。现在前端只有"ACWR 0.7 偏低/不足，可酌情增量"这种模糊表述。

建议加一个数字 + 一句结论（例如"Form +73，处于恢复充分区，适合上质量课"）。

---

## P2 · 实用性增强（按性价比排序）

1. **本周进度条（最该补）** — 现在只有月度和 7/30/90 窗口，没有"本周计划 X 次 / Y km，实际 a 次 / b km"。加一条进度条 + "剩 N 天还能补什么"，是每天真正会看的东西。
2. **跑后分析可选历史场次** — 现在只抓 latest detail。改成抓最近 5–8 次存 `data/raw/activity_details/`，前端加下拉；顺带能画步频 / 触地时间 / 垂直振幅的历史趋势（跑姿改善只有看趋势才有意义）。
3. **主观反馈闭环** — 看板目前纯只读。加一个本地输入区（localStorage）：昨夜主观睡眠、RPE、肌肉酸痛、体重、伤痛标记，参与就绪度打分。这是让看板"变聪明"最便宜的一步，且不需要联网。
4. **HRV/睡眠加个人基线带** — Garmin 已给 `baseline_low/upper`，趋势图加一条浅色"你的正常区间"带，超出才报警，比绝对值有意义。
5. **前端小瑕疵**：
   - `index.html:409` 训练状态 badge 硬编码"进行中"，不跟实际状态变；
   - `index.html:408` 静息心率 >45 时输出一个空 badge（页面上一个空圆块）；
   - 图表没有 tooltip，手机上只能靠猜；
   - 6 个 tab 在手机上占两行，建议吸顶可横向滚动；
   - 没有深色模式。
6. **PWA** — 加 `manifest.json` + `theme-color` + `apple-touch-icon`，手机"添加到主屏"后像 App。零成本。
7. **dist/ 与在线链接落后** — dist 是 9/22 快照，本地数据 9/25。建议把"刷新 → 重拷 dist → 发布"合成一个脚本（发布仍需你授权）。

---

## 建议动手顺序（第二轮）

| 顺序 | 内容 | 理由 |
|---|---|---|
| 1 | C（GBK 崩溃）+ D（today 锚点） | 一个会让同步整条失败，一个会让你照着旧课表跑 |
| 2 | A（plan_config 入库 + defaults 补字段 + 删一条 cron）+ B（恢复/停用自动化二选一） | 目标在看板上现在是残缺的 |
| 3 | E（能力分/状态分拆分） | 评级一天掉一级，会让人不信这个看板 |
| 4 | P2-1 本周进度条 + P2-2 历史场次选择 | 日常最常用 |
| 5 | G（EF 趋势）+ F（证据新鲜度）+ H（TSB） | 回答"我在变强吗 / 今天能不能硬" |
| 6 | 主观反馈闭环 / 基线带 / PWA / 深色模式 | 锦上添花 |

---

*第二轮由小七于 2026-09-25 生成。评审过程中为验证配置丢失问题，已在本地重跑一次 `scripts/analyze.py`（`generated_at` 更新为 2026-09-25 15:47，目标字段已恢复），未改动任何脚本。*

*本文件由小七生成。P0/P1 与大部分 P2 已于 2026-09-22 落地，详见上节。*

